<?xml version="1.0"?>
<odoo>
    <record model="ir.ui.view" id="stock_picking_form_inherit">
        <field name="name">stock.picking.form.inherit</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='location_dest_id']" position="after">
                <field name="sup_de_ruta" string="Super. de Ruta"/>
                <field name="previo" string="Previo"/>
                <field name="ruta" string="Ruta"/>
                <field name="liquidado" string="Liquidado"/>
                <field name="tipo_cliente" string="Tipo Cliente"/>
            </xpath>
        </field>
    </record>
</odoo>

